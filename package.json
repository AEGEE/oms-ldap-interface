{
  "name": "oms-ldap-interface",
  "version": "1.0.0",
  "description": "This is an experiment to try to wrap an ldap interface around the existing core.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ldapjs": "^1.0.2",
    "request": "^2.88.0"
  }
}
